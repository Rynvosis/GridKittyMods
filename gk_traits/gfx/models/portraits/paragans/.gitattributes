# Portrait textures only: 430 files, ~436 MB. Kept out of git history via LFS.
# Scoped to this directory so the small trait icons (2 KB avg) stay in normal git.
*.dds filter=lfs diff=lfs merge=lfs -text
