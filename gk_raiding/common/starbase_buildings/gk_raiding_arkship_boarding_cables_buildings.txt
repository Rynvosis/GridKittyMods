# GK Raiding — Arkship Boarding Cables (A-slot / Auxiliary constructable).
#
# Nomadic counterpart to building_gk_forward_raiding_base (which is is_nomadic = no).
# Built from the arkship build UI in the Utility (A-slot) category, exactly like the
# vanilla arkship_afterburner / arkship hardener buildings. Limit 1 per arkship; equips
# 3 aux Boarding Cables.
#
# Equips GK_ARKSHIP_BOARDING_CABLES (mod variant, common/component_templates/) rather than
# vanilla BOARDING_CABLES_MODULES, because the vanilla component's
# potential = { is_ship_class = shipclass_military } blocks it from equipping on a
# shipclass_starbase arkship. See the component_templates header for the full rationale.
#
# Self-gates DLC: tech_boarding_cables requires Grand Archive (granted by the Despoliation
# finisher's Grand Archive swap, tr_gk_raiding_despoliation_4), and is_arkship_starbase
# requires the Nomads DLC ship_sizes to exist. No extra DLC guard needed.

arkship_boarding_cables = {
	icon = "GFX_ship_part_boarding_cables"   # PLACEHOLDER vanilla sprite — swap for a custom GFX_arkship_boarding_cables when art exists
	starbase_type = arkship
	construction_days = 180
	category = sm_utility_cat

	starbase_limit = {
		base = 1
	}

	potential = {
		owner? = {
			has_technology = tech_boarding_cables
		}
		is_arkship_starbase = yes
	}

	show_in_tech = "tech_boarding_cables"

	show_tech_unlock_if = {
		is_nomadic = yes
	}

	resources = {
		category = starbase_arkship_upgrades
		cost = {
			alloys = 15
		}
	}

	equipped_component = GK_ARKSHIP_BOARDING_CABLES
	equipped_component = GK_ARKSHIP_BOARDING_CABLES
	equipped_component = GK_ARKSHIP_BOARDING_CABLES
	show_component_tooltips = yes
	custom_tooltip = sm_arkship_boarding_cables_tt

	ai_weight = {
		weight = 100 # use same weight here - ai weighting is done in starbase_types.txt
	}
}
