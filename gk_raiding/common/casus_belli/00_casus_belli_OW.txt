# Version last updated: v4.2.3
# Overwrite: cb_despoliation (expanded for all gk_raiding pop-raiding war goals)
# Overwrite: cb_pirate_raid (expanded for gk_raiding resource pillaging)

# Pop raiding CB — all Barbaric Despoiler variants, Nihilistic Acquisition, Emancipators, Robo-Liberators
cb_despoliation = {
	show_in_diplomacy = no
	show_notification = no

	potential = {
		is_country_type = default
		OR = {
			has_valid_civic = civic_barbaric_despoilers
			has_ascension_perk = ap_nihilistic_acquisition
			has_valid_civic = civic_gk_emancipators
			has_valid_civic = civic_gk_emancipators_megacorp
			has_valid_civic = civic_gk_robo_liberators
			has_valid_civic = civic_gk_robo_liberators_megacorp
			has_valid_civic = civic_gk_robo_liberators_machine
			has_valid_civic = civic_gk_barbaric_despoilers_machine
			has_valid_civic = civic_gk_barbaric_despoilers_megacorp
		}
	}

	is_valid = {
		has_total_war_cb = no
		NOT = { is_overlord_to = FROM }
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}

	proxy_war_resources = {
		category = proxy_war
		cost = {
			influence = 100
		}
	}
	on_proxy_war_start = {
		pay_proxy_war_additional_resources = { VALUE = -25 }
	}
}

# Resource pillaging CB — Letters of Marque, pirate flag, Nihilistic Acquisition, Barbaric Despoilers
cb_pirate_raid = {
	show_in_diplomacy = no
	show_notification = no

	potential = {
		is_country_type = default
		OR = {
			has_valid_civic = civic_crusader_spirit_corporate
			has_country_flag = unlocked_pirate_raid_cb
			has_ascension_perk = ap_nihilistic_acquisition
			has_valid_civic = civic_barbaric_despoilers
			has_valid_civic = civic_gk_barbaric_despoilers_machine
			has_valid_civic = civic_gk_barbaric_despoilers_megacorp
		}
	}

	is_valid = {
		has_total_war_cb = no
		NOT = { is_overlord_to = FROM }
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}

	proxy_war_resources = {
		category = proxy_war
		cost = {
			influence = 100
		}
	}
	on_proxy_war_start = {
		pay_proxy_war_additional_resources = { VALUE = -25 }
	}
}
